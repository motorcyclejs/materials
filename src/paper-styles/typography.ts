import '../font-roboto';

import { types } from 'typestyle';

export namespace PaperStylesTypography {

  export const commonBase: types.NestedCSSProperties =
    {
      fontFamily: `'Roboto', 'Noto', sans-serif`,
      '-webkit-font-smoothing': `antialiased`,
    };

}
